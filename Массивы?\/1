Практика #1
Что произойдет в результате выполнения следующего кода?

int a1[] = {1,2,3,4,5};
int a2[] = {6,7,8,9,10};
a1 = a2;

Что произойдет:

После выполнения данного кода происходит следующее:

1. Инициализация массивов:
   - a1 указывает на массив {1, 2, 3, 4, 5}.
   - a2 указывает на массив {6, 7, 8, 9, 10}.

2. Присваивание a1 = a2;:
   - Переменная a1 теперь будет ссылаться на тот же массив, что и a2, то есть {6, 7, 8, 9, 10}.
   - Изначальный массив {1, 2, 3, 4, 5} больше не доступен через переменную a1 (если нет других ссылок на него).

Результат:
После выполнения присваивания обе переменные a1 и a2 ссылаются на массив {6, 7, 8, 9, 10}.
