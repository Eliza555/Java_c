1. Приведите примеры для всех типов данных. Примеры должны также включать нулевые значения.
В языке программирования Java существуют примитивные типы данных и ссылочные типы. Примитивные типы не могут принимать значение `null`, тогда как ссылочные типы — могут. Вот примеры для каждого типа:

Примитивные типы:

- byte
  byte a = 100;
  // Нет возможности присвоить null

- short
  short b = 1000;
  
- int
  int c = 100000;
  
- long
  long d = 100000L;
  
- float
  float e = 10.5f;
  
- double
  double f = 20.5;
  
- char
  char g = 'A';

- boolean
  boolean h = true;

Ссылочные типы (объекты):

- String
  String name = "Елизавета";
  String emptyName = null; // Нулевое значение
  
- Массив
  int[] numbers = {1, 2, 3};
  int[] emptyNumbers = null; // Нулевое значение
  
- Классы и интерфейсы
  Object obj = new Object();
  Object emptyObj = null; // Нулевое значение
