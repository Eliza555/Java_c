Практика #1
Пример перегруженных методов:

Перегрузка методов (overloading) происходит, когда в одном классе несколько методов имеют одинаковое имя, но различаются типами или количеством параметров.

public class Calculator {
    // Перегруженный метод: сложение двух целых чисел
    public int add(int a, int b) {
        return a + b;
    }

    // Перегруженный метод: сложение двух чисел с плавающей точкой
    public double add(double a, double b) {
        return a + b;
    }

    // Перегруженный метод: сложение трех целых чисел
    public int add(int a, int b, int c) {
        return a + b + c;
    }
}


Объяснение:
В классе Calculator метод add перегружен три раза:
1. Принимает два целых числа int.
2. Принимает два числа с плавающей точкой double.
3. Принимает три целых числа int.

Компилятор определяет, какой метод вызвать, исходя из типа и количества переданных аргументов.
