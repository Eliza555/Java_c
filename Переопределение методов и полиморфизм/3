Практика #3
Пример использования аннотации @Override для обнаружения ошибок:

Аннотация @Override помогает компилятору проверить, что метод действительно переопределяет метод из суперкласса. Если метод не переопределяет метод из суперкласса (например, из-за опечатки в имени или неправильных параметров), компилятор выдаст ошибку.

// Суперкласс
class Vehicle {
    public void startEngine() {
        System.out.println("Двигатель запускается");
    }
}

// Подкласс с ошибкой в имени метода
class Car extends Vehicle {
    @Override
    public void strtEngine() { // Опечатка: должен быть startEngine
        System.out.println("Двигатель автомобиля запускается");
    }
}

Объяснение:
В классе Car метод strtEngine содержит опечатку и не совпадает с методом startEngine из суперкласса Vehicle. 
Благодаря аннотации @Override компилятор обнаружит, что метод strtEngine не переопределяет никакой метод из суперкласса, и выдаст ошибку. 
Без аннотации @Override такая ошибка могла бы остаться незамеченной, приводя к непредвиденному поведению программы.
