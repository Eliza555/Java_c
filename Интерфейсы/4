Объявление:

private interface Interface {
  ...
}

Область видимости:

- Топ-уровневый интерфейс: Неправильное объявление. Топ-уровневые интерфейсы не могут быть объявлены с модификатором private. Это приведет к ошибке компиляции.

- Вложенный интерфейс: Внутри класса, модификатор private ограничивает доступ к интерфейсу только самим внешним классом. Внешние классы и другие части программы не смогут напрямую использовать этот интерфейс.

Пример неправильного использования (топ-уровень):

// Ошибка компиляции
private interface MyPrivateInterface {
    void method();
}

Пример правильного использования (вложенный):

public class OuterClass {
    private interface MyPrivateInterface {
        void method();
    }
}
